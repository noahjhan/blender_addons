#  sample unique colors -> hash table
# assign weights to each point proportional to frequency -> normalize
# precalculate pairwise distance -> skip next if a curr center is far away
# WSM algorithm:

# initialization schemes select centers -> look into this

# perhaps use cython?